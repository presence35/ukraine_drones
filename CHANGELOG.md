# Changelog

## [Unreleased]
- Connection: offline retries are now visible — the monitor notification updates with "Offline X/20 min · attempt N" plus the latest reconnect-log line and a progress bar; the 20-min give-up text suggests toggling Airplane mode and adds an "Ignore 30 min" action that pauses all auto-retries (tap the Offline pill / Retry to resume early); the 5-min critical alarm now also lands in the Logs screen's live "Connection log" card above the Offline card. Milestone texts shortened. / З'єднання: спроби перепідключення тепер видно — сповіщення моніторингу оновлюється з «Офлайн X/20 хв · спроба N» та останнім рядком журналу і прогрес-баром; текст відмови на 20-й хвилині радить режим літака та додає кнопку «Ігнорувати 30 хв», що призупиняє всі автоповтори (натисніть «Офлайн»/«Повторити», щоб відновити раніше); критичний сигнал на 5-й хвилині тепер потрапляє в живу картку «Журнал з'єднання» в Логах над карткою «Офлайн». Тексти проміжних етапів скорочено.
- Flourish: tally survives red alerts — tapping "23 threats resolved" after an hour in background replays the last 21, not 3 / Flourish: лічильник зберігається під час тривог — дотик до «Завершено 23 загрози» після години у фоні показує останні 21, а не 3
- Settings: the MiG flyby toggle moved up (right under calm messages), and the "Just Fun" wizard toggle now also covers calm messages / Налаштування: перемикач прольоту МіГ піднято вище (одразу під заспокійливими написами), а перемикач «Розваги» у майстері тепер охоплює і заспокійливі написи.
- Settings: the "City level" option under Oblast alerts dropped its boxed background and has a shorter subtitle / Налаштування: «Міський рівень» у тривогах області більше не має підкладеного фону, а підпис скорочено.
- Official alerts: the note now shows the actual red trident icon followed by "in the header means the official alert is on" / Офіційні тривоги: у примітці тепер справжня червона іконка тризуба з підписом «у шапці означає, що офіційна тривога активна».
- Wizard: "Just Fun" toggle on the last page — one switch for shoot-down animations, the flyby and the tally; purely cosmetic, never affects monitoring or alerts / Майстер: перемикач «Розваги» на останній сторінці — один тумблер для анімацій знешкодження, прольоту літака та лічильника; лише для настрою, не впливає на моніторинг і тривоги.
- Tally notification: no more city badge — the "all Ukraine" mode keeps the 🇺🇦, city mode shows a plain title / Сповіщення лічильника: без іконки міста — режим «вся Україна» зберігає 🇺🇦, а міський режим показує просто заголовок.
- Settings: the "City level" option under Oblast alerts is now a distinct indented subsection with its own background / Налаштування: «Міський рівень» у розділі тривог області тепер окрема виділена підсекція з власним фоном.
- Battery: reconnect attempts now back off when the OS reports no usable network (60s ceiling instead of hammering a dead link), resuming instantly when a connection returns — no change to normal polling / Акумулятор: спроби перепідключення тепер рідшають, коли ОС повідомляє про відсутність мережі (пауза 60 с замість «довбання» мертвого з'єднання) і одразу відновлюються, щойно зв'язок з'явиться — звичайне опитування не змінилося.
- Connection: new orange "degraded" state between online and offline — when the socket is up but the stream has been quiet for 30s the pill turns orange, the monitor notification says threats may be delayed, and the widget badge mirrors it / З'єднання: новий помаранчевий стан «Затримка» між онлайн і офлайн — коли сокет живий, але потік мовчить 30 с, індикатор стає помаранчевим, у сповіщенні моніторингу з'являється попередження про можливу затримку даних, і віджет повторює цей стан.
- Fix: toggling "City level" mid-alert no longer clears the running air-raid notification with a false all-clear and re-rings it when switched back — settings changes never restart an ongoing alert episode / Виправлення: перемикання «Міського рівня» під час тривоги більше не скидає сповіщення з хибним відбоєм і не озвучує його заново при поверненні — зміна налаштувань ніколи не перезапускає тривогу.
- Night mode: the official-alerts description explains it follows the City level setting / Нічний режим: опис офіційних сповіщень пояснює, що діє перемикач «Міський рівень».
- Map: CARTO basemaps now require a (free) API key — the app sends it from a local config, clearing the "API KEY REQUIRED" watermark tiles; © OSM / © CARTO attribution added to the map / Мапа: CARTO тепер вимагає безкоштовний API-ключ — застосунок надсилає його з локального конфіга, водяний знак «API KEY REQUIRED» зник; на мапу додано атрибуцію © OSM / © CARTO.
- Wizard: dedicated completion flag — no map flash before the wizard on first launch, and killing the app during "Replay first launch" no longer resurrects the wizard on every cold start / Майстер: окремий прапорець завершення — немає спалаху мапи перед майстром при першому запуску, а вбивство застосунку під час «Повтору першого запуску» більше не повертає майстра щоразу.
- Settings: the gear hint now pulses instead of spinning (it looked like a loading icon) and stops after 3 opens / Налаштування: підказка на шестерні тепер пульсує замість обертання (схоже було на завантаження) і зникає після 3 відкриттів.
- Fix: with no GPS fix and no pinned city the app now consistently defaults to Odesa — header/banner/widget match what the camera already showed / Виправлення: без GPS-фіксу та закріпленого міста застосунок тепер послідовно показує Одесу — шапка/банер/віджет узгоджені з тим, що вже показувала камера.
- Fix: the CARTO tile key is now appended after `z/x/y.png` (osmdroid builds the path from the base URL), so the key can't land mid-path and 404 the whole map / Виправлення: ключ CARTO тепер додається після `z/x/y.png` (osmdroid формує шлях із базової URL), тож ключ більше не опиняється всередині шляху й не «вбиває» всю мапу.
- Map: attribution moved to bottom-right under the scale bar and shortened to © OSM · © CARTO / Мапа: атрибуцію перенесено вниз праворуч під шкалу масштабу та скорочено до © OSM · © CARTO.
- Settings: the pin-a-city list now only appears when "Follow me (GPS)" is OFF / Налаштування: список «закріпити місто» тепер з'являється лише коли «Слідувати за мною» (GPS) вимкнено.
- Wizard: city-pin chips got breathing room and a clearer selected state; the alert-zones step now edits your real red/yellow radii (not a toy), and shows a static preview of the map's bottom strip (shelter, zone buttons, scale) so you know exactly what to expect / Майстер: чипи вибору міста стали просторішими з чіткішим станом вибору; крок зон тривог тепер змінює справжні червону/жовту зони (не «іграшку») і показує статичний попередній перегляд нижньої частини мапи (укриття, кнопки зон, шкала), тож ви точно знаєте, чого чекати.
- Haptics: every wizard control now ticks on press, and haptics default to the system setting until you choose otherwise / Тактильний відгук: усі елементи майстра тепер вібрують при натисканні, а тактильний відгук за замовчуванням наслідує системний настрій, доки ви не оберете своє.
- Wizard: "Later" is now "Skip"; tips say "threat" instead of "drone"; the settings mention shows the real colored gear icon; city pinning uses tappable city cards (shared with Settings); the zones page mirrors the in-app slider row and labels the red/yellow/gear buttons / Майстер: «Пізніше» тепер «Пропустити»; у підказках «загроза» замість «дрон»; згадка налаштувань — зі справжньою кольоровою іконкою шестерні; прикріплення міста — картки міст (спільні з Налаштуваннями); сторінка зон повторює справжній рядок повзунка, а червона/жовта кнопки й шестерня мають підписи.
- Fix: no more shoot-down haptics/animation while the first-run wizard is on screen — the map yields the focus / Виправлення: під час майстра налаштувань немає вібрації та анімацій знешкодження — мапа віддає фокус.
- Footer: the calm message now reshuffles on every all-clear (random pick, fresh each time threats clear) and nine new encouragements joined the rotation / Панель: заспокійливе повідомлення тепер щоразу випадково оновлюється після відбою, а до ротації додано дев'ять нових підбадьорювань.
- Replay: tapping the "threats resolved" notification goes straight to the show — the map no longer pans home first and waits; the camera jumps onto the targets as soon as it opens / Повтор: дотик до сповіщення «загроз знешкоджено» одразу веде до шоу — мапа більше не панорує додому й не чекає; камера стрибає до цілей щойно застосунок відкрився.
- Footer: during a shoot-down show the copy now reads "Resolving N threats" (per group) instead of "threat X of N" / Панель: під час шоу знешкодження напис тепер «Знешкоджуємо N загроз» (на групу) замість «загрозу X з Y».
- Wizard: new location step — pick "Follow me (GPS)" (requests location permission on tap) or pin your city; Next button now clearly dims while disabled, and progress moved to a thin yellow-blue bar at the button's bottom edge / Майстер налаштування: новий крок локації — «Слідувати за мною» (GPS, із запитом дозволу) або прикріпити місто; кнопка «Далі» тепер явно затемнена, поки недоступна, а прогрес показано тонкою жовто-синьою смужкою внизу кнопки.
- Night mode: the night zone-siren toggle is now titled "Sirens always sound", matching the Alerts section wording / Нічний режим: перемикач нічних сирен тепер називається «Сирена звучить завжди» — узгоджено з формулюванням розділу «Сповіщення».
- Fix: the resolved-threats tally notification is now removed when you "Stop Monitoring & Exit" (or the service dies) — a stale tap or swipe can no longer relaunch the app and silently restart sockets/monitoring. / Виправлення: сповіщення лічильника знешкоджених тепер зникає після «Зупинити моніторинг і вийти» (або зупинки сервісу) — застарілий дотик чи свайп більше не перезапускає застосунок і не вмикає мовчки сокети й моніторинг.
- Perf: tapping a threat now opens the card fast and keeps it fast during live waves. Selection moved out of the shared UI-state into its own popup-only stream, so a tap no longer rebuilds the header, map or footer — only the card; the heavy per-frame threat evaluation runs off the main thread; and the footer strip owns its grouping/sorting in an isolated scope. / Перф: дотик до загрози тепер відкриває картку швидко й не гальмує під час живої хвилі. Вибрану загрозу винесено із спільного UI-стану в окремий потік лише для картки, тож дотик більше не перебудовує шапку, мапу чи нижню панель — тільки картку; важкий погрозовий розрахунок кожного кадру виконується поза головним потоком, а стрічка внизу групує й сортує дані у власній ізольованій області.

- Map: the shelter button is now a 4th circle (fixed 48dp, no text) with the map's teardrop-pin icon, so extra-large fonts can't break the bottom bar — the wizard now shows the same icon too / Мапа: кнопка укриття тепер четверте коло (фіксовані 48dp, без тексту) з іконкою-краплею з мапи, тож надвеликий шрифт більше не ламає нижню панель — та сама іконка тепер і в майстері.
- Fix: opening Settings/Shelters (or backgrounding the app) now instantly cancels a running shoot-down show — bullets erased, camera return cancelled — instead of playing on behind the modal. A queued-but-unstarted replay waits and plays when you're back on the map; the first three times it's interrupted you get a one-line hint ("Shoot-down show waits until you're back on the map"), re-armable via Reset tips. / Виправлення: відкриття Налаштувань/Укриттів (або згортання застосунку) тепер миттєво скасовує шоу, що триває — кулі стерті, повернення камери скасовано, замість гри за модальним вікном. Записаний-у-черзі повтор чекає й грає щойно ви вернетесь на мапу; перші три рази переривання ви отримаєте підказку («Шоу знешкодження почекає, доки ви не повернетеся на мапу»), яку можна скинути через «Скинути підказки».

- Replay polish: every target of a group now stands on-screen BEFORE the camera jumps to it (pre-spawned with staggered fire times, flights re-edged to the new viewport), and follow-the-bullet camera moves are suppressed while a show runs — so "return home" can no longer be dragged off by a competing animation. / Полірування повтору: усі цілі групи вже стоять на екрані ДО стрибка камери до них (розставлені наперед зі зміщеними часами пострілів, траєкторії переорієнтовані на новий огляд), а рухи камери «за снарядом» вимкнені під час шоу — тож «повернення додому» більше не збивається конкуруючою анімацією.

- Alerts: MiG-31K takeoff alerts finally ring — an aviation threat is never locally expired anymore (only NEPTUN's own «stale» retires it), and while its bell is on a takeoff rings the red zone country-wide, regardless of distance or your fast-zone thresholds. / Сповіщення: тривоги про злет МіГ-31К нарешті спрацьовують — авіаційну загрозу більше не «протухає» локально (її знімає лише серверне «stale»), а з увімкненим дзвіночком злет дзвонить у червоній зоні на всю країну, незалежно від відстані та ваших порогів швидких зон.

- Map: new MiG-31K flyby — when the alert fires, the full-size plane crosses the whole screen on the bearing from its airbase toward you with a contrail, and the threat card opens when it lands. / Мапа: новий прольот МіГ-31К — коли спрацьовує тривога, літак у повний розмір перетинає весь екран за азимутом від свого аеродрому на вас, зі слідом конденсату, а після приземлення відкривається картка загрози.

- Perf: tapping a drone is instant now — the haptic ticks on touch itself (at the marker, before anything composes), and the threat card renders in a single frame: all card entrance/size/body animations are gone, selection motion is the title-icon pop alone. / Перф: дотик до дрона тепер миттєвий — вібрація спрацьовує на самому дотику (на маркері, до будь-якої композиції), а картка загрози малюється за один кадр: усі анімації появи/розміру/тіла картки прибрано, з руху лишився лише «стрибок» іконки заголовка.

- Replay: every target of the next group already stands on-screen before its bullets fly (targets pre-spawn when the camera lands), and the camera pans to the next group right after the last bullet hits — no explosion wait. Only the final group plays the full blast. / Повтор: усі цілі наступної групи вже стоять на екрані до польоту куль (цілі попередньо розставляються при прибуттї камери), а камера переходить до наступної групи одразу після влучання останньої кулі — без очікування вибуху. Повну анімацію грає лише фінальна група.

- Settings · System & Display: new "City labels" row with two chips — Mid-size and Small — controlling whether medium-city labels (Маріуполь, Краматорськ…) and small-town labels appear on the map; both on by default. / Налаштування · Система та дисплей: новий рядок «Міста на карті» з двома чипами — «Середні» та «Дрібні» — керує показом підписів середніх міст (Маріуполь, Краматорськ…) і дрібних міст на мапі; обидва типово увімкнені.
- Fix: the shoot-down replay now returns the camera to where you actually were — a live osmdroid point was held instead of a snapshot, so "return home" sometimes landed on a random spot. The show is also much faster: intermediate groups only show the bullets hitting (brief flash, no lingering explosions), the camera jumps to each group instead of gliding while bullets already fly, and only the final group gets the full animation. The Debug log's shoot-down entry reads like text now ("Shots: 21 · Groups: 10") instead of "21x10". / Виправлення: повтор шоу знешкодження тепер повертає камеру туди, де ви справді були — замість знімка тримався «живий» осмдроїд-поінт, тож «повернення додому» часом приземлялося на випадкове місце. Шоу також значно швидше: проміжні групи показують лише влучання снарядів (короткий спалах без довгих вибухів), камера стрибає до групи, а не ковзає, поки кулі вже летять, і лише фінальна група отримує повну анімацію. Запис шоу в журналі налагодження читається як текст («Пострілів: 21 · Груп: 10») замість «21x10».

- Threat card: the large card now animates — it grows in on appear and its size glides when you toggle SMALL ↔ LARGE via the stepper (instant snap when system animations are off). / Картка загрози: велика картка тепер анімується — виростає при появі, а її розмір плавно змінюється при перемиканні МАЛА ↔ ВЕЛИКА степером (миттєво, коли системні анімації вимкнено).

- Tally notification: emoji scope badge (🇺🇦 when counting all of Ukraine, 🏙️ for local) prefixes the title, and the body now lists a per-type breakdown ("UAV 3 · Cruise missile 1") instead of the old "Last: … · scope" line. / Сповіщення-лічильник: емодзі охоплення (🇺🇦 для всієї України, 🏙️ для локального) на початку заголовка, а в тілі — розбивка за типами («БпЛА 3 · Крилата ракета 1») замість старого рядка «Остання: … · охоплення».
- Perf: eliminated the recomposition storm during heavy NEPTUN stream activity. UI now samples the NEPTUN feed at 120 ms (sample(120)) and moves the 1 s wall-clock tick out of UiState into a dedicated StateFlow; StateFlow dedup then drops no-op emissions, so a quiet feed triggers zero recompositions and even a live wave caps at ~8 rebuilds/s. ThreatPopupCard's elapsed-time clock moved to a leaf composable so the card body no longer recomposes every second. MapView's overlayKey is memoized via remember(keyDeps) instead of rebuilding on every frame. Key model classes (Threat, ThreatProximity, City, ZoneParams, LatLng, NightConfig, ZoneArmed, NightZones, RevealRequest, FlourishShow, ReplayProgress, TrailPoint) marked @Immutable to enable skipping. / Перф: усунено бурю рекомпозицій під час інтенсивного потоку NEPTUN. UI тепер семплює NEPTUN-стрічку на 120 мс (sample(120)) і виносить 1-секундний годинник з UiState в окремий StateFlow; дедуп StateFlow відкидає порожні емісії — у спокої ніяких рекомпозицій, а під час хвилі жорсткий ліміт ~8 перебудов/с. Годинник elapsed-time у ThreatPopupCard винесено в листовий компонент, щоб тіло картки не перемальовувалося кожну секунду. overlayKey в MapView мемоізається через remember(keyDeps) замість перебудови кожен кадр. Ключові моделі (Threat, ThreatProximity, City, ZoneParams, LatLng, NightConfig, ZoneArmed, NightZones, RevealRequest, FlourishShow, ReplayProgress, TrailPoint) позначені @Immutable для увімкнення пропуску.

- Zones editor: now slides up from the bottom edge like the connection sheet instead of popping in, and slides back down on close (snaps instantly when system animations are removed); the 🌙 emoji is dropped from the "Night zones" header. / Редактор зон тепер виїжджає з нижнього краю, як аркуш з'єднання, замість стрибка, й заїжджає назад при закритті (миттєво, коли системні анімації вимкнено); емодзі 🌙 прибрано із заголовка «Нічні зони».

- Map: the shoot-down flourish is now much smoother and self-explanatory. The explosion glow is pre-rendered once instead of allocating a gradient shader every frame, marker icons are cached, and the show redraws at 30fps instead of 60 — killing the jank even with several bullets in flight. While a replay runs the footer swaps in "Resolving threat X of N" plus a slim amber progress bar along its bottom edge, then the threat strip returns when the show ends. / Мапа: шоу знешкодження тепер значно плавніше й зрозуміліше. Сяйво вибуху попередньо рендериться один раз замість створення градієнтного шейдера щокадрово, іконки маркерів кешуються, а шоу малюється на 30 fps замість 60 — прибрано гальма навіть із кількома кулями в польоті. Поки триває повтор, у нижній панелі з'являється «Знешкоджуємо загрозу X з Y» та тонкий бурштиновий індикатор прогресу по нижньому краю, а після завершення повертається стрічка загроз.

- Map: Південне (Odesa oblast) now shows under its current name, and four more pre-2024 renames are applied (Шептицький, Звягель, Златопіль, Берестин). City coverage grew to ~483 places: every Ukrainian town of 10k+ from GeoNames joined the curated set (2 km dedupe), and large non-oblast-seat cities like Маріуполь, Краматорськ or Сімферополь get their labels already at mid-zoom instead of waiting for the closest zoom level. / Мапа: Південне на Одещині показується під актуальною назвою, застосовано ще чотири перед-2024 перейменування (Шептицький, Звягель, Златопіль, Берестин). Покриття міст зросло до ~483: до кураторського набору приєднано всі міста України від 10 тис. мешканців із GeoNames (дедуплікація в радіусі 2 км), а великі не-обласні міста — як Маріуполь, Краматорськ чи Сімферополь — отримують підписи вже за середнього наближення, а не лише на самому близькому.

- Fix: entering the shelters overlay no longer self-cancels when system animations are on — the zoom-to-fit animation's intermediate zoom levels no longer trip the auto-exit gate, so the shelter pins stay put instead of flashing on then hiding. / Виправлення: відкриття оверлея укриттів більше не скасовує себе за увімкнених системних анімацій — проміжні рівні зуму під час анімації підгонки більше не спрацьовують на автоматичний вихід, тож піни укриттів залишаються, а не блимають і не зникають.

- Settings · unknown threat: the "Schrödinger's object" joke now sits below the cat picture (it no longer overlaps), the leading dash is dropped, and the picture is 15% smaller. / Налаштування · невідома загроза: жарт «об'єкт Шредінгера» тепер під картинкою з котом (більше не накладається), тире на початку прибрано, а картинку зменшено на 15%.

- Threat card: with system animations removed ("Remove animations" / zero animator scale) the popup now snaps in with no transition machinery at all, and the selection icon-pop is skipped — only the haptic tick remains; with animations on, the pop now plays after the body slide-in lands (bigger 0.4→1 bounce) so it's actually visible. / Картка загрози: коли в системі вимкнено анімації («Видалити анімації» / нульовий масштаб аніматора), картка з'являється миттєво без перехідної машини й «стрик» іконки пропускається — лиш тактильний відгук; з увімкненими анімаціями стрик грає після приземлення ковзання тіла (більший відскік 0,4→1), тож його справді видно.

- Threat card opens instantly: the 300 ms fade-in is now a ~100 ms fade (dismiss keeps its smooth fade), and switching threats pops the title icon with a haptic tick plus a short slide-fade of the card body, so it's always clear which threat got selected. / Картка загрози з'являється миттєво: замість 300 мс затемнення — швидке на ~100 мс (закриття лишилось плавним), а під час зміни загрози іконка в заголовку «стрикає» з тактильним відгуком і коротким ковзанням тіла картки — завжди зрозуміло, яку загрозу вибрано.

- Map: removed the grey "last seen" dots that appeared next to shoot-down explosions; the explosion itself already shows where a threat died. Stale-threat dimming is unchanged. / Мапа: прибрано сірі крапки «востаннє бачили», що з'являлися поруч із вибухами; сам вибух уже показує, де загроза знищена. Затемнення застарілих загроз не змінилось.

- Fix: the shoot-down replay no longer loses bullets or double-counts. NEPTUN re-sent resolutions were remembered twice — two bullets flying at the same threat and an inflated tally; duplicates are now filtered by removal id. A full 21-record show no longer silently drops bullets past the animation cap, the footer's "Resolving threat X of N" now replaces even the threat strip while a replay runs (the strip returns when it ends), and the Debug log records each show as records×groups so grouping is diagnosable. / Виправлення: повтор шоу знешкодження більше не втрачає снарядів і не рахує двічі. NEPTUN повторно надсилав повідомлення про знешкодження — вони записувались двічі: дві кулі в одну ціль і завищений лічильник; тепер дублікати відфільтровуються за ідентифікатором. Повне шоу на 21 запис більше не губить снаряди через ліміт анімації, напис «Знешкоджуємо загрозу X з Y» під час повтору заміщує навіть стрічку загроз (стрічка повертається по завершенні), а журнал налагодження записує кожне шоу як «записи×групи» для діагностики.

- Settings · Just Fun: new footnote explains how the flourish works — pure fun with zero effect on monitoring or alerts, replays stay available during a live alert (your own tap) but a brand-new alert instantly clears a running show, and the official siren always outranks the fireworks. / Налаштування · Просто розваги: нова примітка пояснює, як працює розвага — жодного впливу на моніторинг чи сповіщення, повтор доступний і під час тривоги (це ваш власний дотик), але нова тривога миттєво прибирає шоу, що триває, а офіційна сирена завжди важливіша за феєрверки.

- Fix: an official oblast alert now rings exactly once per alert episode — no repeats. All three re-ring paths are closed: (1) Android killing and restarting the alert service mid-alert no longer re-rings it (the announced episode survives restarts in prefs, loaded race-free before the first tick); (2) toggling official alerts off/on no longer re-announces an alert that already rang — an alert that starts while muted still announces on re-enable; (3) a new drone in the oblast no longer re-posts the alert if you tapped or swiped it away — the reason-text refresh stays silent or is skipped, never a brand-new siren. / Виправлено: офіційна тривога області тепер звучить рівно один раз за епізод — без повторів. Закрито всі три шляхи повтору: (1) вбивство та перезапуск сервісу сповіщень Android під час тривоги більше не озвучує її повторно (оголошений епізод зберігається в налаштуваннях і завантажується без гонки до першого такту); (2) вимкнення та повторне ввімкнення офіційних тривог більше не озвучує тривогу, що вже лунала — тривога, що почалась під час вимкнення, усе ще озвучується при ввімкненні; (3) новий дрон в області більше не відновлює сповіщення, якщо ви вже натиснули або прибрали його — оновлення тексту причини лишається тихим або пропускається, але ніколи не стає новою сиреною.

- Fix: the shoot-down replay now plays even while an official alert is live in your area — it's an explicit tap, so it no longer drops silently with a confusing "another alert posted first" log line. A brand-new alert onset mid-show still clears it so the alarm owns the screen. / Виправлення: повтор шоу знешкодження тепер грає навіть під час офіційної тривоги у вашій зоні — це свідомий дотик користувача, тож більше не зникає мовчки з незрозумілим записом «інша тривога вийшла першою» в журналі. Якщо нова тривога починається посеред шоу, воно досі зникає, щоб тривога володіла екраном.

- Popup card: the course line is hidden when it only repeats the type and location already shown under the title (e.g. "UAV - Kobleve, Mykolaiv oblast" over a "Kobleve · Mykolaiv oblast" header); real sentences like "patrolling over the sea" still show. Small card: title now aligns with the big card's title, the reliability letter is larger and the skull smaller. / Картка загрози: рядок курсу ховається, якщо він лише повторює тип і локацію з-під заголовка (напр. «БпЛА - Коблеве, Миколаївська область» над шапкою «Коблеве · Миколаївська область»); справжні речення на кшталт «патрулює над морем» лишаються. Мала картка: заголовок вирівняно як на великій картці, літера надійності більша, череп менший.

- Fix: tapping the "N threats resolved" tally notification no longer silently drops the replay — the show now waits out transient states (cold start, Settings open, shelter overlay) and plays the moment the map is actually visible; with the Shoot-down animation toggle off it explains itself via a toast, and a live alert in your zone still cancels it (safety first, now audited). The Debug log gains a "Shoot-down show" entry recording each tap's outcome. / Виправлення: натискання сповіщення «N загроз знешкоджено» більше не мовчки втрачає повтор — шоу чекає на перехідні стани (холодний старт, відкриті налаштування, укриття) і грає щойно мапа справді видима; якщо вимкнено «Анімацію знешкодження», застосунок пояснює це тостом, а активна тривога у вашій зоні досі скасовує показ (безпека понад усе, тепер із записом у журналі). Журнал налагодження отримує запис «Шоу знешкодження» із результатом кожного натискання.

- Settings: "Haptic feedback" moved from Just Fun to System & Display, and press ticks now actually vibrate — they fire through the same always-on vibrator channel as the shoot-down flourish (the system touch-feedback API is ignored/muted by most devices), and press detection rides each control's own interaction source (the same signal as its press animation), so a tick lands on every tap. / Налаштування: «Тактильний відгук» переїхав із «Просто розваги» до «Системи та інтерфейсу», і натискання тепер справді вібрують — через той самий завжди-активний віброканал, що й анімація збиття (системне API тактильного відгуку більшість пристроїв ігнорує або глушить), а спрацьовування визначає власне джерело взаємодії кожної кнопки (той самий сигнал, що й анімація натискання), тож імпульс надходить на кожен дотик.

- Settings: the "City level" alert-scope toggle (and the other nested rows under icon'd toggles) is now properly indented under its parent, so it reads as a subset of "Official alerts". The small threat card hugs its content when GPS is off (no more full-width border stretch). The footer now shows the calm message / "No relevant threats" after a long-press shoot-down instead of repeating the popup's "Wishful thinking…". / Налаштування: перемикач «Міський рівень» (та інші вкладені рядки під перемикачами з іконками) тепер правильно відступає під батьківським рядком і виглядає підмножиною «Офіційних тривог». Маленька картка загрози обіймає свій вміст, коли GPS вимкнено (більше не розтягується на всю ширину рамкою). Нижня панель після збиття довгим натисканням тепер показує заспокійливе повідомлення / «Немає актуальних загроз» замість повтору «Тільки бажання…» з картки.
- Logs: rebuilt for regular users — 24h subtitle, Notified as right-edged chip (on=only notified, off=both), Shoot-downs chip hidden by default, sort as "Newest/Oldest" chip instead of rotating icon, tabs show counts / Журнал: перероблено для звичайних користувачів — підпис 24 год, «Сповіщені» як чип праворуч (увімк — лише сповіщені, вимк — усі), чип «Знешкодження» вимкнено типово, сортування як чип «Новіші/Старіші» замість іконки, вкладки з лічильниками.

