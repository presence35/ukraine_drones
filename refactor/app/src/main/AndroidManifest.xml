<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Foreground Service Permissions for Android 14+ / 16 (API 36) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Location Permissions (Coarse baseline for battery efficiency, fine when available) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Network & Telemetry Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Direct Boot and Post-Reboot Monitoring Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Notifications for Critical Threat Alerts (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:name=".OkoNebaApp"
        android:allowBackup="false"
        android:icon="@android:drawable/ic_dialog_alert"
        android:label="OkoNeba Core"
        android:supportsRtl="true">

        <!-- Android 16 Location Foreground Service -->
        <service
            android:name="ua.okoneba.feature.alerts.service.AlertService"
            android:directBootAware="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Direct Boot Pre-Unlock Receiver -->
        <receiver
            android:name="ua.okoneba.app.receiver.DirectBootReceiver"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Post-Unlock User Receiver -->
        <receiver
            android:name="ua.okoneba.app.receiver.UserUnlockReceiver"
            android:directBootAware="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
